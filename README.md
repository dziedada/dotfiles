# Dotfiles and other configuration files for Linux

## Contains:

* `.bashrc` Settings for bash terminal
* `.bash_aliases` Some useful (and funny) aliases
* `.ssh_config` Config file for known hosts
* `.vimrc` vim config file
